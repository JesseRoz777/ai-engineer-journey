# AI Engineer Journey

Day 2 — Python fundamentals: variables, data types, lists, dictionaries, and control flow.

`hello.py` stores a list of customer dictionaries and prints the name of each customer whose `active` value is `True`.
